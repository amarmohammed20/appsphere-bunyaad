# pnpm v11 reads only auth and registry settings from .npmrc.
# Everything else lives in pnpm-workspace.yaml.

# Lock installs to the official registry — blocks dependency confusion.
registry=https://registry.npmjs.org/
